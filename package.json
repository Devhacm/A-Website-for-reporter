{
  "name": "reporterhq",
  "version": "1.0.0",
  "description": "Simple news reporter website with admin panel and backend API.",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js --watch server.js --watch data.json"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
